# 阿里云大模型 ACP 认证教程 — 核心知识点总结

> 基于项目 `aliyun_acp_learning` 全部教程内容（C1~C5）整理

---

## 目录

- [第一章 课程准备](#第一章-课程准备)
- [第二章 构造问答系统](#第二章-构造问答系统)
- [第三章 构建 Agent 系统](#第三章-构建-agent-系统)
- [第四章 交付上线](#第四章-交付上线)
- [第五章 总结与展望](#第五章-总结与展望)
- [关键术语汇总](#关键术语汇总)

---

## 第一章 课程准备

### 1.1 学习环境搭建

| 环境类型 | 特点 | 适用场景 |
|---------|------|---------|
| 本地 IDE | 需自有 Python 环境，下载代码+安装依赖 | 有 Python 环境的学员 |
| ModelScope Notebook | 免费，单次连续 4 小时后自动关闭 | 零成本学习 |
| PAI-DSW | 实例可保留，按"计算时"计费 | 需持续学习的学员 |

### 1.2 PAI-DSW 计费规则

- 免费试用按"计算时"抵扣，3 个月内每月 250 计算时，总计 750 计算时
- 关闭浏览器 ≠ 停止实例，必须手动"停止"或"删除"
- **停止**：中止 CPU/GPU 计费，保留存储
- **删除**：彻底释放所有资源，中止所有计费

### 1.3 CPU vs GPU 选择策略

| 课程阶段 | 推荐实例 | 说明 |
|---------|---------|------|
| 1.x、2.x、3.x | ecs.g6.xlarge (CPU) | 默认选择 |
| 4.1（蒸馏/微调）、4.2（部署） | ecs.gn7i-c8g1.2xlarge (A10 GPU) | 需 GPU |

### 1.4 Python 虚拟环境与内核管理

- 使用 `venv` 创建独立虚拟环境 `llm_learn`
- 通过 `ipykernel` 将虚拟环境注册为 Jupyter 内核
- 命令：`python -m ipykernel install --user --name llm_learn --display-name "Python (llm_learn)"`

---

## 第二章 构造问答系统

### 2.1 项目背景

教育内容开发公司面临新员工答疑需求频繁的问题，传统问答系统基于固定规则和相似度匹配，局限性大，决定采用大模型技术构建智能问答应用。

### 2.2 用大模型构建新人答疑机器人

#### 2.2.1 OpenAI SDK 调用千问大模型

- 使用 `OpenAI` 客户端，`base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"`
- **messages 参数结构**：
  - `system`：设置大模型角色和任务
  - `user`：用户问题
  - `assistant`：模型回复
- **多轮对话**：维护 `conversation_history` 列表，将历史对话全部传入 messages
- **流式输出**：`stream=True`，迭代 `chunk.choices[0].delta.content`

#### 2.2.2 大模型文本生成工作流程（五阶段）

1. **文本分词（Tokenization）**：文本 → Token ID 序列
2. **Token 向量化**：通过 Embedding 矩阵查表为向量，添加位置编码
3. **大模型推理**：多层 Decoder Block（因果自注意力 + 前馈网络）→ 隐藏状态 → Logits
4. **解码与自回归**：Softmax → 概率分布 → 解码策略（贪心/Beam Search/Top-p/Top-k）→ 自回归生成
5. **输出文本**：Token ID 序列解码为字符串

#### 2.2.3 随机性控制参数

| 参数 | 范围 | 默认值 | 作用 |
|------|------|--------|------|
| `temperature` | [0, 2) | 0.7 | 调节 softmax 概率分布尖锐程度；低→确定，高→多样 |
| `top_p` | (0, 1] | 0.8 | 按概率降序选取累计概率达阈值的 Token 子集 |
| `top_k` | 正整数 | - | 只保留概率最高的 k 个 Token |
| `seed` | 整数 | - | 传入相同值尽量返回相同结果（不保证完全一致） |

> **重要建议**：不要同时调整 temperature 和 top_p，可能导致输出不可预测

#### 2.2.4 上下文工程（Context Engineering）

- **上下文窗口（Context Window）**：大模型接收输入的地方，容量有限，类比 RAM
- **四大核心技术**：RAG（检索增强）、Prompt（提示词工程）、Tool（工具使用）、Memory（记忆机制）

#### 2.2.5 RAG 技术方案

**两阶段流程**：
1. **建立索引**：文档解析 → 文本分段 → 文本向量化 → 存储索引
2. **检索与生成**：用户问题向量化 → 语义相似度比较 → 召回相关片段 → 与问题合并传给大模型

### 2.3 扩展答疑机器人的知识范围

#### 2.3.1 RAG 工作原理（详细）

**建立索引四步骤**：
1. **文档解析**：PDF/Word/Markdown → Document 对象
2. **文本分段**：长文档切分为小段落
3. **文本向量化**：Embedding 模型转为向量
4. **存储索引**：向量存入向量数据库

**检索生成两步骤**：
1. **检索**：用户问题向量化 → 语义相似度比较 → 召回最相关段落
2. **生成**：问题 + 召回文本段 → 提示词模板 → 大模型生成回复

#### 2.3.2 LlamaIndex 核心 API

- `SimpleDirectoryReader`：加载目录中的文件
- `VectorStoreIndex.from_documents(documents, embed_model=...)`：创建索引
- `index.as_query_engine(streaming=True, llm=...)`：创建查询引擎
- `DashScopeEmbedding`：阿里云 Embedding 模型
- 索引持久化：`index.storage_context.persist(path)` / `load_index_from_storage()`

#### 2.3.3 RAG 多轮对话与问题改写

- **挑战**：用户问题依赖对话历史上下文
- **解决方案**：利用大模型根据对话历史改写当前问题
- **LlamaIndex 实现**：`CondenseQuestionChatEngine`

#### 2.3.4 文本向量化原理

- Embedding 模型通过**对比学习**训练
- 建立索引阶段：n 个 chunk → n 个向量 → 向量数据库
- 检索阶段：问题 q → 向量 vq → 找最相似的 n 个向量 → 返回对应文本段

### 2.4 优化提示词改善答疑机器人回答质量

#### 2.4.1 提示词框架（六要素）

| 要素 | 含义 |
|------|------|
| 任务目标（Object） | 明确要求大模型完成什么任务 |
| 上下文（Context） | 任务的背景信息 |
| 角色（Role） | 大模型扮演的身份/语气/写作风格 |
| 受众（Audience） | 目标用户群体，约束应答风格 |
| 样例（Sample） | 参考案例，引导输出格式和风格 |
| 输出格式（Output Format） | 指定输出格式、类型、枚举范围 |

#### 2.4.2 提示词模板

- LlamaIndex 默认模板含 `{context_str}` 和 `{query_str}` 变量
- 通过 `PromptTemplate` 和 `query_engine.update_prompts()` 自定义

#### 2.4.3 提示词技巧

1. **清晰表达需求 + 分隔符**：`【】`、`<< >>`、`###`、`===`、`---`、XML 标签
2. **限定角色和受众**：不同角色产生不同风格和深度
3. **规定输出格式**：JSON、Markdown 等结构化输出
4. **少样本示例（Few-shot）**：提供样例让大模型模仿
5. **思维链（COT）**：让模型"一步步思考"，提升复杂任务准确率

#### 2.4.4 Meta Prompting

- 让大模型扮演"提示词评审专家"分析并优化提示词
- 多轮迭代流程：设定参考答案 → 生成回答 → 差距分析 → 重写提示词 → 重复

#### 2.4.5 AI 裁判（AI Judge）

- 训练流程类似机器学习分类器：准备标注样本 → 拆分训练/评估集 → 预测 → 找错题 → Meta Prompting 优化 → 监控过拟合

#### 2.4.6 推理大模型

- **特点**：输出包含完整推导步骤，适合数学解题、代码编写
- **提示词技巧**：保持简洁，避免思维链提示（推理模型自带），让推理模型充当"提示词教练"
- **混合使用策略**：推理模型做规划/决策，通用模型做执行

### 2.5 自动化评测答疑机器人的表现

#### 2.5.1 RAG 评测框架对比

| 框架 | 核心特点 | 主要指标 |
|------|---------|---------|
| Ragas | 让大模型评估 RAG 系统 | Answer Correctness, Answer Relevancy, Faithfulness, Context Precision, Context Recall |
| TruLens | 专注可观测性 | Groundedness, Answer Relevance, Context Relevance |
| DeepEval | 引入单元测试和 TDD | Faithfulness, Answer Relevance, Context Relevance, Context Recall |

#### 2.5.2 Ragas 指标体系（重点）

**整体回答质量**：
- **Answer Correctness** = 0.25 × 语义相似度 + 0.75 × 事实准确度
  - 事实准确度：F1 = TP / (TP + 0.5 × (FP + FN))

**生成环节**：
- **Answer Relevancy**：答案是否与问题相关
- **Faithfulness**：答案与检索上下文的事实一致性（检测幻觉）

**召回环节**：
- **Context Precision**：相关 context 是否排名靠前、占比高（信噪比）
- **Context Recall**：ground_truth 中多少比例的观点能被 contexts 支持

#### 2.5.3 根据 Ragas 指标进行优化

| 指标低 | 问题阶段 | 优化方向 |
|--------|---------|---------|
| Context Recall 低 | 检索阶段 | 检查知识库完备性、更换 embedding 模型、query 改写 |
| Context Precision 低 | 检索阶段 | 以上措施 + 加入 Rerank 重排序 |
| Answer Correctness 低（前两项高） | 生成阶段 | 优化 prompt、调整 temperature、更换更强模型、微调 |

#### 2.5.4 Lost in the Middle 现象

关键信息被埋藏在大量无关信息中，大模型"视而不见"。高质量上下文应具有高信息密度、低噪音（高知识浓度）。

### 2.6 优化 RAG 应用提升问答准确度

#### 2.6.1 文档解析与切片

**五种切片方法对比**：

| 切片方法 | 特点 | 适用场景 |
|---------|------|---------|
| TokenTextSplitter | 按 Token 数量固定切分 | 严格控制 Token 长度 |
| SentenceSplitter（默认） | 保持句子完整性 | 大多数场景 |
| SentenceWindowNodeParser | 每个切片包含周围句子作为上下文窗口 | 需要保持上下文 |
| SemanticSplitterNodeParser | 根据语义相关性自适应切分 | 专业性强文档 |
| MarkdownNodeParser | 按 Markdown 标题层级切分 | Markdown 文档 |

#### 2.6.2 检索前优化（7 种策略）

1. **问题改写**：将简短/模糊问题改写为完整问题
2. **问题扩写**：增加更多信息让检索结果更全面
3. **基于用户画像扩展上下文**：结合用户信息扩写问题
4. **提取标签**：从问题中提取结构化标签，用于标签过滤 + 向量检索
5. **反问用户**：问题不明确时让大模型反问
6. **多步骤查询**：`StepDecomposeQueryTransform` + `MultiStepQueryEngine`
7. **HyDE（假设文档嵌入）**：先生成"假想答案文档" → 用假想文档检索真实文档

#### 2.6.3 检索后优化

1. **重排序（Rerank）**：先大量召回 → 用重排序模型精排 → 筛选最相关 top_n 条
2. **滑动窗口检索**：检索到一个切片后补充前后相邻切片

#### 2.6.4 生成答案阶段优化

- **选择合适的大模型**：简单查询 qwen-turbo，复杂推理 qwen-plus/max，长文档 qwen-long
- **优化提示词模板**：明确要求不编造答案，添加内容分隔标记
- **调整大模型参数**：temperature、top_p、seed、presence_penalty、max_tokens

---

## 第三章 构建 Agent 系统

### 3.1 Agent 基础与工具调用

#### 3.1.1 工具调用演进路径

硬编码 → 关键词匹配路由 → 大模型意图识别 → 结构化输出 → Function Calling → MCP 协议

#### 3.1.2 Function Calling（函数调用）

**工作原理（三步）**：
1. **工具定义**：在 API 的 `tools` 参数中用 JSON Schema 描述 `name`、`description`、`parameters`
2. **调用决策**：模型返回 `tool_calls` 字段，含函数名和参数 JSON
3. **执行与返回**：开发者解析→执行函数→将结果包装为 `role: "tool"` 的 message→再次调用 API

**关键 API 参数**：`tools`（工具定义列表）、`tool_choice`（`"auto"` 让模型自动决策）、`tool_call_id`（关联调用与返回）

#### 3.1.3 ReAct 模式

- **思考（Thought）**：模型推理当前状态
- **行动（Action）**：调用工具
- **观察（Observation）**：获取工具返回结果
- 循环直到任务完成

#### 3.1.4 AgentScope 框架

核心组件：`ReActAgent`（内置 ReAct 循环）、`Toolkit`（工具管理器）、`DashScopeChatModel`（通义千问封装）

#### 3.1.5 MCP 协议（Model Context Protocol）

- **核心思想**："谁提供工具，谁定义工具"，将工具定义与使用解耦
- **MCP Server**（提供方）声明工具，**MCP Client**（消费方）动态发现并拉取工具
- **两种传输方式**：stdio（本地通信）、Streamable HTTP（远程通信）

### 3.2 让 Agent 学会规划与执行

#### 3.2.1 反思（Reflection）机制

**两种反思模式**：
- **自我反馈（Self Review）**：单步指令式反思 / 两步式"生成-反馈"
- **外部反馈（External Feedback）**：代码执行验证、图表渲染验证、计算验证、结构化校验

#### 3.2.2 工作流五种编排模式

| 模式 | 特点 | 适用场景 |
|------|------|---------|
| 流水线（Pipeline） | 固定顺序执行 | 业务流程固定的任务 |
| 分支选择（Branching） | 路由器引导到不同路径 | 智能客服、多工具 Agent |
| 并行执行（Parallel） | 互不依赖子任务同时处理 | 复杂审阅、综合报告 |
| 混合专家（MoA） | 多个不同模型并行处理同一任务 | 开放性/创造性任务 |
| 人机协作（HITL） | 设计暂停节点引入人类决策 | 高价值操作审批 |

#### 3.2.3 自主规划（Planning）

- Agent 从"任务执行者"升级为"解决方案规划师"
- **代码生成（Code as Action）**：用编程语言表达复杂逻辑
- **动态创造工具**：提供代码解释器作为"元工具"
- **"探索-固化"混合模式**：探索阶段用自主规划找方案，固化阶段封装为固定工作流

### 3.3 用多 Agent 实现团队协作

#### 3.3.1 两种协作模式

| 模式 | 结构 | 特点 | 适用场景 |
|------|------|------|---------|
| 分层规划模式 | 中心化星型（Leader-Member） | Leader 分解分派汇总，上下文隔离 | 目标明确、可清晰分解的任务 |
| 共创协作模式 | 去中心化（黑板模式） | 多专家围绕共享空间头脑风暴 | 开放性创造性问题 |

#### 3.3.2 AgentScope 实现

- **分层规划**：handoff 机制，Leader 将专家 Agent 作为工具调用
- **共创协作**：`MsgHub`，消息自动广播给所有参与者

### 3.4 用 Memory 让 Agent 积累经验

#### 3.4.1 大模型的无状态性

每次 API 调用都是独立的，调用结束后彻底忘记一切。朴素方案（每次发送完整对话历史）存在三大问题：上下文窗口限制、成本急剧上升、性能下降（注意力稀释、中间迷失）。

#### 3.4.2 三种记忆管理策略

| 策略 | 思路 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|---------|
| 固定窗口截断 | 只保留最近 N 轮 | 实现简单 | 一刀切失忆 | 信息价值快速衰减 |
| 滚动摘要 | 旧对话压缩为摘要 | 保留核心信息 | 额外 API 成本 | 需长期连贯性 |
| 向量化召回 | 对话转向量存储，按语义检索 | 精准回忆 | 系统复杂度高 | 长期智能交互 |

#### 3.4.3 AgentScope 长期记忆

- **static_control（静态控制）**：自动被动保存和检索记忆
- **agent_control（Agent 控制）**：Agent 获得 `record_to_memory` 和 `retrieve_from_memory` 工具，自主决定何时记忆

#### 3.4.4 短期 vs 长期记忆

| 维度 | 短期记忆 | 长期记忆 |
|------|----------|----------|
| 基于 | 对话缓冲区 | 向量数据库 |
| 策略 | 截断/摘要 | 向量化召回 |
| 职责 | 维持当前会话连贯性 | 持久化存储+跨会话检索 |

---

## 第四章 交付上线

### 4.1 用蒸馏让小模型掌握专业能力

#### 4.1.1 蒸馏 vs 微调

| | 微调 | 蒸馏 |
|---|---|---|
| 数据来源 | 人工标注 | 教师模型生成 |
| 数据成本 | 高 | 低 |
| 训练流程 | SFT | SFT（增加数据构建步骤） |

#### 4.1.2 三条蒸馏路径

| 路径 | 别名 | 需要什么 | 适用场景 |
|---|---|---|---|
| 数据合成蒸馏 | 黑盒蒸馏 | 只需教师 API | 结构化任务 |
| 知识蒸馏 KD | 白盒蒸馏 | 需要教师权重 | 开源教师、需更高精度 |
| 推理压缩 | - | 需要教师推理输出 | 多步推理任务 |

#### 4.1.3 LoRA 微调原理

- 不修改原始权重，在旁边"挂"一对低秩矩阵学习变化量
- 原始权重矩阵 W（d×d），LoRA 分解 ΔW = A×B，A 为 d×r，B 为 r×d，r 远小于 d
- 当 d=4096, r=8 时，参数量从 16,777,216 减少到 65,536（0.4%）
- 保留模型 97% 以上基础能力，避免"灾难性遗忘"

#### 4.1.4 训练状态判断

| 训练损失 | 验证损失 | 状态 |
|---------|---------|------|
| 不降/上升 | 不降/上升 | 训练失败（学习率太高）|
| 下降 | 下降 | 欠拟合（继续训练）|
| 下降 | 先降后升 | 过拟合（需更多数据或减少轮次）|
| 趋于平稳 | 趋于平稳 | 训练成功 |

#### 4.1.5 ms-swift LoRA 微调关键参数

| 参数 | 选值 | 调优方向 |
|------|------|---------|
| lora_rank | 8 | 数据<200 降到 4；>2000 升到 16 |
| learning_rate | 5e-5 | loss 不降→调高；震荡→调低 |
| num_train_epochs | 5 | 验证损失上升→减少 |
| per_device_train_batch_size | 8 | 显存不足→减小 |
| max_length | 512 | 根据 token 长度 P95 决定 |

### 4.2 部署模型

#### 4.2.1 vLLM 部署

vLLM 是专为大模型推理设计的开源框架，通过内存优化和缓存策略提升推理速度和支持高并发。

关键命令：
```bash
vllm serve "/path/to/model" --load-format "safetensors" --port 8000 --gpu-memory-utilization 0.7
```

兼容 OpenAI API：支持 `/v1/chat/completions` 和 `/v1/models` 接口。

#### 4.2.2 云上部署方式对比

| 服务 | 特点 | 适用场景 |
|------|------|---------|
| 百炼 | 一键部署，封装底层复杂性 | 快速部署千问系列 |
| 函数计算 FC | Serverless GPU，按请求量计费 | 轻量级推理、低频访问 |
| PAI-EAS | 弹性扩缩容、蓝绿部署 | 实时同步推理 |
| GPU 云服务器 ECS | IaaS 层，灵活安装 | 自定义模型训练/推理 |
| 容器服务 ACK/ACS | K8s 集群部署 | 复杂微服务架构 |

### 4.3 大模型应用生产实践

#### 4.3.1 SLO 关键指标

- **TTFT（Time to First Token）**：首 token 延迟
- **TPOT（Time per Output Token）**：每 token 生成时间

#### 4.3.2 五大性能提升原则

1. **更快处理请求**：模型小型化、模型压缩（剪枝/量化/蒸馏）、优化提示词
2. **减少请求数和运算量**：上下文缓存（Context Cache）、批处理
3. **减少 Token 输入输出**：精简输入、提示词引导 vs max_tokens（安全阀）
4. **并行化处理**：请求级并行、批处理、模型并行/张量并行
5. **不要默认依赖大模型**：硬编码标准响应、利用经典 UI 组件和传统算法

#### 4.3.3 成本优化

三种计费方式：预付费（包年包月，最低成本）、按量付费（中等）、抢占式实例 spot（最高节约 90%）

#### 4.3.4 可观测性

- **OpenTelemetry 三类数据**：Metrics（指标）、Traces（链路追踪）、Logs（日志）
- **ARMS 三个维度**：成本（Token 分析）、性能（调用链分析）、质量与安全

### 4.4 大模型应用安全合规

#### 4.4.1 典型攻击类型

- **提示词注入（Prompt Injection）**：通过角色扮演诱导模型绕过安全限制
- **指令注入（Command Injection）**：利用 Agent 工具调用注入恶意指令
- **提示词泄漏（Prompt Leaking）**：诱导模型泄露系统提示词
- **幻觉（Hallucination）**：模型捏造不真实内容
- **数据投毒（Data Poisoning）**：向训练数据植入后门

#### 4.4.2 阿里云 AI 安全护栏

- **接入方式**：API/SDK 直接调用、百炼集成（请求头 `X-DashScope-DataInspection`）、WAF/AI 网关集成
- **防护范围**：文本内容审查、图像内容审查（多模态）

#### 4.4.3 AI 安全纵深防御体系

**五步构建**：盘点资产 → 加固地基 → 保护模型 → 加固应用 → 合规备案

**基础设施安全四项基本防护**：网络隔离（VPC）、系统加固、静态数据加密（KMS）、最小权限原则（RAM）

**下一代安全理念**：零信任、可信计算、机密计算（TEE）、远程证明

#### 4.4.4 RAG 安全防护

- **第一道防线**：知识库访问控制（根据用户权限过滤 Top-K 文档）
- **第二道防线**：知识库双重加密（AES-256 文本加密 + DCPE 向量加密）

#### 4.4.5 应用实时三层防御

1. **输入过滤**：AI 安全护栏拦截越狱、角色扮演、提示词注入
2. **运行监控**：审计 Agent 行为，拦截恶意功能调用
3. **输出审查**：拦截幻觉、有害言论、数据泄露

#### 4.4.6 合规与备案

- **中国**：《生成式人工智能服务管理暂行办法》（2023.8.15），未完成算法备案面临下架风险
- **欧盟**：《人工智能法案》(AI Act)，基于风险等级监管
- **美国**：NIST《AI 风险管理框架》，行业引导为主

---

## 第五章 总结与展望

### 5.1 RIDE 方法论

业务应用 AI 的四阶段方法论：
- **R** — 选方向：识别哪些业务场景适合用 AI 切入
- **I** — 定标准：为 AI 应用建立可衡量的标准
- **D** — 推落地：推动 AI 系统在实际业务中落地
- **E** — 验效果：验证 AI 应用的业务效果

### 5.2 可体验的 AI 技术解决方案

1. 多模态数据信息提取
2. AI 剧本生成与动画创作
3. 10 分钟构建 AI 智能助手（多端部署）
4. AI 实时音视频通话
5. 与 AI 程序员结伴编程

### 5.3 AI 行业落地案例

| 领域 | 案例 |
|------|------|
| 医疗 | 药品不良反应报告生成、CT 影像辅助诊断（20 秒，96%+准确率）|
| 娱乐 | 虚拟偶像出演真人剧集、Vtuber 虚拟主播 |
| 教育 | AI 一对一老师，个性化学习方案 |

---

## 关键术语汇总

| 术语 | 定义 |
|------|------|
| Tokenization | 将文本编码为 Token（子词片段）ID 序列 |
| Embedding | 将 Token ID 转为包含语义信息的高维向量 |
| Autoregressive Generation | 自回归生成，逐 token 预测并追加到输入序列 |
| Context Window | 大模型接收输入的容量有限的区域 |
| RAG | 检索增强生成，先检索相关片段再生成答案 |
| Lost in the Middle | 关键信息被埋藏在大量无关信息中被模型忽略 |
| HyDE | 假设文档嵌入，先生成假想答案再检索真实文档 |
| Rerank | 重排序，对初步检索结果用专门模型精排 |
| Faithfulness | 答案与检索上下文的事实一致性 |
| Hallucination | 大模型捏造不真实内容的现象 |
| COT | 思维链，让模型逐步推理输出中间步骤 |
| Meta Prompting | 让大模型帮助优化提示词本身的方法 |
| Function Calling | 大模型 API 内置的工具调用能力 |
| ReAct | 思考-行动-观察的循环模式 |
| MCP | 模型上下文协议，将工具定义与使用解耦 |
| LoRA | 低秩自适应微调，只训练极小部分参数 |
| 蒸馏 | 把大模型的判断能力"复制"给小模型 |
| vLLM | 大模型推理开源框架，支持内存优化和高并发 |
| SLO | 服务化级别目标 |
| TTFT | 首 token 延迟 |
| TPOT | 每 token 生成时间 |
| Context Cache | 上下文缓存，缓存公共前缀减少重复计算 |
| TEE | 可信执行环境，CPU 硬件加密隔离区 |
| 零信任 | 永不信任，始终验证的安全理念 |
| Prompt Injection | 通过注入新指令覆盖或劫持模型原有任务 |
| DCPE 加密 | 保留向量近似距离关系的加密算法 |
